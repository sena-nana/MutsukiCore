[build]
# Avoid inheriting a stale machine-specific absolute path.
rustc-wrapper = "sccache"

[alias]
c = "check --workspace --all-targets"
t = "test --workspace --all-targets"
lint = "clippy --workspace --all-targets -- -D warnings"
timings = "build --workspace --timings"
bench-full = "run --release --package mutsuki-runtime-benchmarks -- full --gate release --baseline artifacts/perf/issue28-baseline.json"
bench-smoke = "run --release --package mutsuki-runtime-benchmarks -- smoke --gate smoke"
