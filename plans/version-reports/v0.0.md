# v0.0 版本报告

## 方向

建立 MutsukiBot 项目宪法、分层、契约草案与规则文档，作为后续实现的**唯一事实来源**。本版本不写任何实现代码（`mutsukibot/` 与 `tests/` 目录不创建）。

## 完成项

| 文件 | 说明 |
|---|---|
| [AGENTS.md](../../AGENTS.md) | 项目宪法 + 阅读顺序 + 12 条不可违反的最高规则 |
| [README.md](../../README.md) | 一句话定位 + 阅读入口索引 |
| [pyproject.toml](../../pyproject.toml) | 最小工程配置：Python 3.13 + ruff + pyright；不锁运行时 / dev 依赖 |
| [plans/roadmap.md](../roadmap.md) | 当前版本目标、范围、门控、后续版本方向、红线、同步规则 |
| [plans/architecture.md](../architecture.md) | 项目方向、Agent 一等公民、分层、与 Yume / mind-sim 关系、拆解风险与预埋接口、领域中立机制、数据流参考 |
| [plans/engineering.md](../engineering.md) | 技术栈、目录结构、插件模型、12 条横切公约实现规则、测试基础设施、lint / 类型 / Git 工作流 |
| [plans/contracts.md](../contracts.md) | 内部协议草案：核心契约对象、PluginManifest、Error、Capability、ServiceMode、事务原语、PluginScope、Trace、决定性时间、Schema 协议、通用 By-Ref Payload 协议、承载性测试样本 |

## 基线

- 仓库无 `mutsukibot/` 目录、无 `tests/` 目录，符合 v0.0「不写实现」红线。
- 无运行时依赖；`pyproject.toml` 仅声明工具链配置。
- 所有 plans 文档不含实现代码或锁死的 API 形态描述。

## 运行检查

N/A —— v0.0 无可运行代码。仅做文档自检：

- [x] AGENTS.md 阅读顺序中列出的 5 份文档全部存在且可达。
- [x] [plans/contracts.md](../contracts.md) 章节编号 §1 → §12 连续无断裂。
- [x] [plans/engineering.md §4.1](../engineering.md) capability 已知名清单与 [plans/contracts.md §4](../contracts.md) 一致（包含 `hold_ref` / `borrow_ref` / `produce_ref_stream`）。
- [x] [plans/architecture.md §3](../architecture.md) 分层图明确区分 import 依赖与 observability 旁路订阅。

## 效果检查

逐项核对 [plans/roadmap.md](../roadmap.md) v0.0 验收标准：

| 验收项 | 证据 |
|---|---|
| ① MutsukiBot 是什么 / 不是什么 | [AGENTS.md](../../AGENTS.md) 一句话定位；[architecture.md §1](../architecture.md) 项目方向；[architecture.md §3](../architecture.md) 反模式 |
| ② 与 Koishi / NoneBot / AstrBot 的借鉴边界 | [architecture.md §1 借鉴对象与边界表](../architecture.md)；[AGENTS.md](../../AGENTS.md) Hard Rule 12「Borrow with Discipline」 |
| ③ 与 Yume / mind-sim 的关系 | [architecture.md §4](../architecture.md) 命名空间建议 + 承载性测试样本；[contracts.md §12](../contracts.md) 承载性测试样本 |
| ④ Yume / mind-sim 为何能拆插件，以及拆解的风险与对策 | [architecture.md §5](../architecture.md) 10 条风险与 v0.0 预埋机制对照表；[architecture.md §6](../architecture.md) 领域中立机制 |
| ⑤ 下一步做什么（v0.1 范围） | [roadmap.md「下一版本：v0.1 最小可运行骨架」](../roadmap.md) 候选范围 + 门控 |

文档自身**不包含实现代码或 API 形态描述**：已通过逐文档复核。

## 下版门槛（v0.1）

进入 v0.1 需满足 [plans/roadmap.md「v0.1 门控」](../roadmap.md)：

- 一个 Agent 能 spawn → awake → 处理一条 echo → sleep → stop。
- echo 插件能被人类触发，也能作为 LLM tool 被调用（验证「指令即工具」hard rule）。
- 热重载 echo 插件 100 次后无资源泄漏（验证 `PluginScope` 完整回收）。
- 所有横切公约 lint 规则就位。
- Yume v0.4 的某个 `StimulusEvent → ExpressionDecision` 样本能用 v0.1 契约表达（序列化 / 反序列化通过，`RefPayload` 字段除外）。
- 通用 By-Ref 协议自洽：stub 引用模拟「插件 A 产生 ref → 插件 B 借用 ref → scope 关闭自动释放」链路，全程核心代码与 trace 字段不出现 `latent` / `tensor` / `kv` / `gpu` 字样。

v0.1 候选范围见 [plans/roadmap.md](../roadmap.md)。
